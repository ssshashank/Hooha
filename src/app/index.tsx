import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const LandingScreen = () => {
    return (
        <SafeAreaView>
            <View>
                <Text>Landing Roote</Text>
            </View>
        </SafeAreaView>);
};

export default LandingScreen;
